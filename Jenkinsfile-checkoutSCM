pipeline {
    agent any

    stages {
         stage('Clean SCM') {
            steps {
                cleanWs()
            }
        }
        stage('Checkout SCM') {
            steps {
                sh 'git clone https://github.com/Weys230/Projet-m1res-2526.git'
            }
        }
    }
}
